import React from 'react'
import Header from '../components/user/Header'

const Session = () => {
    return (
        <div>
            <Header tab={`Session`} />
            session

        </div>
    )
}

export default Session
